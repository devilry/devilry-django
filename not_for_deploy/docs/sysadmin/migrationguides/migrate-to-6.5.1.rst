=========================
Migrating to 6.5.1
=========================

.. warning:: Always update one version at a time. Do not skip versions unless it is explicitly stated in the migration guide.


Backup database and files
#########################

BACKUP. YOUR. DATABASE. AND. FILES.


What's new?
###########

- Update `cradmin_legacy` library, which in turn removes dependency on the old `future` library.


Update devilry
##############

Update the devilry version to ``6.5.1`` as described in :doc:`../update`.
