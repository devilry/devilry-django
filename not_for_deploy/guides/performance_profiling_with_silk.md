# Performance profiling with django-silk

This is already setup in develop settings. Just need to go to http://localhost:8000/silk/ to view the profiling data.

To clear profiling data, run the following command:

```bash
python manage.py silk_clear_request_log
```
