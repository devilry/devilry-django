The CSS is compiled from style.scss using SASS (http://sass-lang.com/).

Install sass with:
    ~$ gem install sass

Compile continously while editing style.scss with:
    ~$ sass --watch style.scss:style.css
