The CSS is compiled from style.scss using SASS (http://sass-lang.com/).

Install sass with:
    ~$ gem install sass

Compile:
    ~$ sass common.scss common.css

Compile continously while editing common.scss with:
    ~$ sass --watch common.scss:common.css
