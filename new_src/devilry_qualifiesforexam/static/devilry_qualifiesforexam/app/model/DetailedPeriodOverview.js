/**
 * Wrapper for the DetailedPeriodOverview model in ``devilry_subjectadmin.
 * We have a wrapper to make it easier to use the model in a controller.
 * */
Ext.define('devilry_qualifiesforexam.model.DetailedPeriodOverview', {
    extend: 'devilry_subjectadmin.model.DetailedPeriodOverview'
});
